name: devtools_desktop
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.0.1
  google_fonts: ^6.3.3
  intl: ^0.20.2
  provider: ^6.1.5+1
  shared_preferences: ^2.5.4
  uuid: ^4.5.2
  window_manager: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
